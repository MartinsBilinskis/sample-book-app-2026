name: 'Build Application/Service'
description: 'Build Sample Book App service'
runs:
  using: composite
  steps:
    - run: echo "Installing all necessary node dependencies.."
      shell: pwsh
    - name: Install dependecies
      run: npm install
      shell: pwsh
    - run: echo "Dependecies successfully installed.."
      shell: pwsh



#      echo "Testing Sample Book Application service has started on ${environment} environment.."
#      git branch: 'main', poll:
#                            false, url: 'https://github.com/MartinsBilinskis/RTU-sample-API-automation-2026-CI-DI.git'
#      pwsh "npm install"
#      pwsh "npm run books BOOKS_${environment}"
#      echo "Testing Sample Book Application service finished.."